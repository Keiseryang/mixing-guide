# 新手调音师入门教学网页 · 实施计划

## 一、目标
做一个面向「零基础/新手调音师」的单页教学网站，用通俗语言 + 图示把最核心的基础讲明白。
只讲「最基本的混音」，不深入复杂效果器，侧重安全意识与动手前的准备。

## 二、参考与风格
参考本机已有项目 `FOH学习网页` 的视觉风格（用户偏好）：
- 深色主色调 + 可切换浅色主题（`data-theme` 切换）
- 顶栏 sticky 导航，文字锚点滚动
- 卡片式内容、醒目标题色、清晰分隔
- **手机端自适应**（用户习惯用手机访问，会配套局域网访问脚本）

## 三、项目位置与文件
保存到 `C:\Users\Keiser\Desktop\.pi\projects\调音入门教学\`，包含：
- `index.html` —— 主网页（内嵌 CSS/JS，单文件便携，可直接双击打开）
- `server.js` —— 简易本地服务器（支持手机在同一 WiFi 下访问，可选）
- `启动手机访问.bat` —— Windows 一键启动
- `使用说明.txt` —— 使用方法
- `plan.md` —— 本计划

## 四、网页内容结构（板块）

### 板块一：线材的认识与管理收纳
- 认识常见线材：XLR（卡侬线）、TRS（大三芯/平衡）、TS（大二芯/非平衡）、RCA（莲花）、USB、音箱线
- 平衡 vs 非平衡的通俗概念（长线抗干扰用平衡）
- **如何管理收纳**：上电前理线、捆扎带、绕线手法（8字绕法防打结）、贴标签编号、线盘/线袋收纳、防踩踏防缠绕

### 板块二：接口的认识
- 输入端：XLR（3 针）、TRS（6.35mm 大三芯）、TS（6.35mm 大二芯）、RCA
- 输出端：XLR、TRS、RCA、耳机口（3.5mm / 6.35mm）
- 调音台通道条上常见的接口位置
- 数字接口（USB、同轴、光纤）简述
- 每条接口配简单示意（SVG 图形）与用途

### 板块三：麦克风认识与 48V 幻象电源
- 三种常用麦克风：动圈 / 电容 / 铝带（通俗对比）
- **什么麦克风需要 48V**：电容麦、有源 DI、需幻象供电的主动设备
- **什么不需要 48V**：动圈麦（如 SM58）、无源 DI、乐器…（接错一般不坏但无声/异常）
- 何时开 / 关 48V 的正确时序（先静音/关主输出，再开 48V）
- 安全提示：热插拔注意、非平衡设备别乱接幻象电、拔线前确认 48V 已关

### 板块四：接入调音台前的准备与防止爆音
- **开机顺序**：先开调音台（及其他前级/音源），最后开功放/音箱；关机相反 → 防开/关机瞬间从音箱爆音
- 接线前：推子拉底、增益旋钮归零、先接输入再接输出、确认 48V 状态
- 用 **PFL/Solo 耳机监听** 试电平，别用主输出大音量试
- 防爆音要点：先降增益、避免扫频增益过高、别热插拔有源设备、别在接线上突然切换、别敲击/移动话筒产生瞬态
- 检查清单：线材接触、幻象电、设备电源、增益/推子位置

### 板块五（补充）新手还需知道的事
1. **增益级联（Gain Staging）**：先看表设增益，再推推子，层层管理电平——混音的地基
2. **认识调音台面板**：通道条（GAIN / EQ / AUX / PAN / FADER）、母线段（主输出、编组）简图
3. **监听与听力保护**：耳机/音箱音量要低，保护听力，别在扩音系统下大声
4. **啸叫（Feedback）预防与处理**：别对着话筒说话、降监听增益、EQ 削频
5. **常见故障排查**：无信号 / 声音小 / 底噪电流声 / 啸叫 / 单声道 / 相位，按顺序排查
6. **混音第一步（基础）**：先让整体可听清——音量平衡、声像摆放、简单高低通（只点到为止，不深入效果器）
7. **现场安全与礼仪**：理线防绊倒、设备防水防摔、开关机顺序、台面整洁、标签编号
8. **正确接线习惯**：从左到右、一步步来、用耳机实时确认

## 五、界面与实现
- 顶部导航（锚点）：线材 / 接口 / 麦克风48V / 防爆音 / 新手必知
- 每板块：标题 + 简介 + 「要点卡片」 + 「图示（SVG）」 + 「警示/提示框」
- 侧边或顶部「明暗主题」切换按钮（跟随用户偏好保留 FOH 深浅两套配色）
- 底部「快速检查清单」可勾选，方便新手实操前逐项确认
- 全部内容为简体中文，避免生硬术语，必要时给通俗比喻

## 六、验收标准
1. 双击 `index.html` 即可打开，内容完整、无报错、无外部依赖
2. 手机端自适应良好，文字/卡片不溢出
3. 明暗主题切换正常，导航锚点滚动正常
4. 四大核心板块全部覆盖，补充第 5 板块，含实操检查清单
